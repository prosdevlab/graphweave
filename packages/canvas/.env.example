# API key for the execution service.
# Generate one with (from repo root):
#   docker compose -f docker-compose.dev.yml exec execution uv run python -m app.cli create-key --name admin --scopes all
VITE_API_KEY=
